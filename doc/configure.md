# configure.sh  
_Uses a hash and alias to quickly edit config files (less typing)_  

---  

# Setup  
1. Edit the configure.sh script `nf configure`  
2. modify or view the `config_file` bash key-pair  
3. if changed, reload the shell `rs`  
4. Edit the config file with `cfg <application>` eg. `cfg dunst`  
